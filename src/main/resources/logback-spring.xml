<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Structured JSON logging. Every line carries service name, timestamp, level,
         message and the MDC fields populated by Micrometer Tracing (traceId, spanId). -->
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="account-service"/>

    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- MDC (traceId/spanId) is included by default -->
            <includeMdc>true</includeMdc>
            <customFields>{"service":"${appName}"}</customFields>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <level>level</level>
                <thread>thread</thread>
                <logger>logger</logger>
                <message>message</message>
                <version>[ignore]</version>
            </fieldNames>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="JSON"/>
    </root>
</configuration>
